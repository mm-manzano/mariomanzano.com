{
  "scripts": {
    "dev": "vite --host",
    "build": "vite-ssg build",
    "preview": "vite preview --host",
    "check": "tsc --noEmit",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "vite-ssg": "^0.24.3",
    "jsdom": "^26.0.0",
    ... (keep your other devDeps)
  }
}
