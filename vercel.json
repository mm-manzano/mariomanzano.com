{
  "buildCommand": "pnpm install --frozen-lockfile && cd client && pnpm install --frozen-lockfile && pnpm run build",
  "outputDirectory": "client/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "env": {
    "RESEND_API_KEY": "@resend_api_key"
  },
  "functions": {
    "api/**/*.mjs": {
      "runtime": "nodejs20.x"
    }
  }
}
